[defaults]
# ensure the repository roles/ folder is searched (and keep playbooks/roles as fallback)
roles_path = ./roles:./playbooks/roles:~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles

# optional: use your local hosts.ini by default
inventory = hosts.ini